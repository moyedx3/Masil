import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable if needed for World App webview
  // async headers() {
  //   return [
  //     {
  //       source: "/:path*",
  //       headers: [
  //         { key: "Access-Control-Allow-Origin", value: "*" },
  //       ],
  //     },
  //   ];
  // },
};

export default nextConfig;
